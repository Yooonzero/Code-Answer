function solution(message) {
    var answer = 0;
    a = message.length;
    b = message.length * 2;
    // answer = `message의 글자 수가 ${a}개로 최소 가로 ${b}cm의 편지지가 필요합니다.`;
    answer = b
    return answer;
}